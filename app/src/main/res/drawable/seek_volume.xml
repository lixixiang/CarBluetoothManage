<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:startColor="@color/beacon_rssi"
                android:centerColor="@color/beacon_rssi"
                android:centerY="0.75"
                android:endColor="@color/beacon_rssi"
                android:angle="270"/>
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:startColor="@color/blue"
                    android:centerY="0.75"
                    android:centerColor="@color/blue2"
                    android:endColor="@color/blue3"
                    android:angle="270"/>
            </shape>
        </clip>
    </item>

    <!-- 第二进度条 -->
    <!--  <item android:id="@android:id/secondaryProgress">
         <clip>
             <shape>
                 <corners android:radius="5dp" />

                 <gradient
                     android:angle="270"
                     android:centerColor="#545454"
                     android:centerY="0.75"
                     android:endColor="#545454"
                     android:startColor="#545454" />
             </shape>
         </clip>
     </item> -->
</layer-list>
